document.write("<h1>Factorial</h1>");
var N = prompt("Introduce N:");
var P = Math.sqrt(2 * Math.PI * N);
var T = Math.pow(N / Math.E, N);
var S = P * T;
document.write("<h3>El factorial es: " + S + "</h3>");
